#!/usr/bin/env bash
# Tuple room-joined trigger. Delegates to the shared shell runtime.
set -euo pipefail
here="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
exec "${here}/run-trigger.sh" room-joined "$@"
